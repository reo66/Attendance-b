<!DOCTYPE html>
<html>
  <head>
  <!--yieldメソッドを呼び出している。これにより、ブラウザのテンプレ部にタイトルがでる（メソッドはヘルパーで定義したもの）-->
    <title><%= full_title(yield(:title)) %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 
                               'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 
                               'data-turbolinks-track': 'reload' %>
   <!--<!Internet Explorerのバージョン別の指示のコードを、'layouts/shim'から呼び出す-->
   <%= render 'layouts/shim' %>
  </head>
  <body>
   <!--headerの内容を'layouts/header'から呼び出している-->
   <%= render 'layouts/header'%>
  <!--各<body>の内容をcontainerによって中心に配置-->
  <div class="container">
    <%= yield %>
     <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>

